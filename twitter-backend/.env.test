# Configuración específica para tests E2E
NODE_ENV=test

# MongoDB será configurado dinámicamente por MongoDB Memory Server
# Esta variable será sobrescrita en setup.ts
MONGODB_URI=mongodb://localhost:27017/test_twitter_db

# JWT Configuration para tests
JWT_SECRET=test-jwt-secret-key-for-e2e-tests
JWT_EXPIRES_IN=1h

# Configuración de logs para tests (menos verbose)
LOG_LEVEL=error

# Configuración específica para tests
TEST_MODE=true